Vega thumbnail example:

```js
const chartSpec = require('mocks/vega-spec.json');
const theme = require('mocks/vega-theme.json');
<VegaThumbnail width={300} height={250} spec={chartSpec} config={theme} />
```
